# Python 3.12.3
# Pinned to what's actually imported — not a full `pip freeze` (which would
# include ~20 transitive CUDA wheels pulled in by torch).
# PDB parsing is hand-rolled (src/bindembed_addon/structure/extractor.py);
# no biopython/biotite dependency.
torch==2.12.1+cu126
transformers==5.12.1
sentencepiece==0.2.1
h5py==3.16.0
numpy==2.5.0
pandas==3.0.3
scikit-learn==1.9.0
